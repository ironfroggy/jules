<html>
    <title>{{ meta.title }}</title>
    <body>
        <h1>{{ meta.title }}</h1>
        <ul>
            {% for tag in engine.tags %}
                <li>{{ tag }}</li>
            {% endfor %}
        </ul>
    </body>
</html>
